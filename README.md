# Spicetify-Startup-Script
Spicetify CLI Auto-Installer for Windows. This script creates a persistent system-level task to execute the Spicetify installation command (iwr ... | iex) silently on every login, ensuring your Spicetify installation is always up-to-date without manual intervention.
